# This is a public portfolio with only public dependencies, so it pins the
# open npm registry for both local installs and Vercel deploys. This overrides
# any machine-global private registry (e.g. JFrog) for THIS repo only, and
# keeps the lockfile's resolved URLs pointing at registry.npmjs.org.
registry=https://registry.npmjs.org/
